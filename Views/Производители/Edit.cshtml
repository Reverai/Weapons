@model Weapons.Models.Производители

@{
    ViewData["Title"] = "Изменить";
}

<h1>Изменить</h1>

<h4>Производители</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Код_производителя" />
            <div class="form-group">
                <label asp-for="Наименование_производителя" class="control-label"></label>
                <input asp-for="Наименование_производителя" class="form-control" />
                <span asp-validation-for="Наименование_производителя" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
