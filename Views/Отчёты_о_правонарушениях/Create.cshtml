@model Weapons.Models.Отчёты_о_правонарушениях

@{
    ViewData["Title"] = "Создать";
}

<h1>Создать</h1>

<h4>Отчёты_о_правонарушениях</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Вид_правонарушения" class="control-label"></label>
                <input asp-for="Вид_правонарушения" class="form-control" />
                <span asp-validation-for="Вид_правонарушения" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Дата_правонарушения" class="control-label"></label>
                <input asp-for="Дата_правонарушения" class="form-control" />
                <span asp-validation-for="Дата_правонарушения" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Создать" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Назад</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
